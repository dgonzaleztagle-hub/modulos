{
  "name": "modulos-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Workspace tecnico para MODULOS",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests",
    "check": "powershell -ExecutionPolicy Bypass -File scripts/doublecheck.ps1",
    "viewer": "node scripts/serve-viewer.mjs"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
